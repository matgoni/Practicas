const searchInput = document.querySelector("input[name='search']");
const filterSelect = document.querySelector("select[name='filter']");
const applyButton = document.querySelector("input[value='Aplicar']");
const filterButton = document.querySelector("input[value='Filtrar']");
const tableBody = document.querySelector("tbody");

searchInput.addEventListener("input", () => {

});

// Filtrar por género al presionar el botón "Filtrar"
filterButton.addEventListener("click", () => {

});

// Eliminar registros al presionar el botón "Aplicar" después de seleccionar "Eliminar"
applyButton.addEventListener("click", () => {

});

// Función para filtrar por título
const filterByTitle = (term) => {

};

// Función para filtrar por género
const filterByGenre = (genre) => {

};

// Función para eliminar registros seleccionados
const deleteSelectedRecords = () => {

};
  
  